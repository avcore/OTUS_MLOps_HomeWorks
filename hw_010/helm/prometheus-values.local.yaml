alertmanager:
  config:
    receivers:
      # ВАЖНО: имя должно совпадать
      - name: 'null'
      - name: "tg-admin"
        telegram_configs:
          - bot_token: "8683142088:AAFuc6nQo3dzMGvF8Wgmf0k9652cQ8Fcy-k"
            chat_id: -5291416131
            send_resolved: true
            parse_mode: "HTML"
            message: |
              <b>🚨 {{ .CommonLabels.alertname }}</b>
              <i>severity:</i> {{ .CommonLabels.severity }}
              {{ range .Alerts -}}
              • {{ .Annotations.summary }}
              {{ end }}
